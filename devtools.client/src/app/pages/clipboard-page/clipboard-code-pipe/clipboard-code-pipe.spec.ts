import { ClipboardCodePipe } from './clipboard-code-pipe';

describe('ClipboardCodePipe', () => {
  it('create an instance', () => {
    const pipe = new ClipboardCodePipe();
    expect(pipe).toBeTruthy();
  });
});
